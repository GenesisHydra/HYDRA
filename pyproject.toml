[tool.poetry]
name = "hydra"
version = "0.1.0"
description = "HYDRA 1 – Revenue‑generating autonomous AI system"
authors = ["Genesis Team"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.115.0"
uvicorn = {extras = ["standard"], version = "^0.30.0"}
httpx = "^0.27"
pydantic = "^2.8"

[tool.poetry.dev-dependencies]
pytest = "^8.3"
pytest-asyncio = "^0.24"
